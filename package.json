{
  "name": "run-bus-tracker-pwa",
  "version": "1.0.0",
  "description": "Admin/user PWA for storing and viewing run and bus numbers",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.2.1",
    "xlsx": "^0.18.3"
  }
}
